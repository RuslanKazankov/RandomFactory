<Window x:Class="RandomFactory.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RandomFactory" xmlns:viewmodels="clr-namespace:RandomFactory.ViewModels"
        mc:Ignorable="d"
        Title="RandomFactory" Height="800" Width="800">
    <Window.DataContext>
        <viewmodels:MainViewModel/>
    </Window.DataContext>
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock FontSize="60" Text="{Binding ResultOfGeneration}" TextAlignment="Center"/>
            <Button Command="{Binding GenerateIntCommand}" Content="Сгенерировать целое число"/>
            <Button Command="{Binding GenerateDoubleCommand}" Content="Сгенерировать вещественное число"/>
            <Button Command="{Binding GeneratePercentCommand}" Content="Сгенерировать процент"/>
        </StackPanel>
    </Grid>
</Window>
